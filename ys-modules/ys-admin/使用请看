# 默认的profile为dev，其他环境通过指定启动参数使用不同的profile，比如：  
生产环境--spring.profiles.active=prod  
测试环境--spring.profiles.active=test  

1.新建一个名为test的数据库，执行test.sql
2.运行生产者和消费者启动类，访问：http://localhost:8080/swagger-ui.html 即可看到RESTful API的页面 
3.访问：http://localhost:8082/druid 即可看到sql 监控

